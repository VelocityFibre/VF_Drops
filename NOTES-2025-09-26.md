# Work-in-Progress Notes — 2025-09-26 (Fri)

Goal: Add an agent assignment dropdown next to Project in the Installations grid with options: Unallocated (default), Zander, Michael — persisted to Neon.

Changes made today:
- DB: Added installations.assigned_agent (default 'Unallocated') with CHECK constraint and index.
- API: /api/installations now selects assigned_agent, supports filtering by assigned_agent, and added PUT to update assigned_agent by id.
- UI: InstallationsGrid shows an Assigned Agent column (after Project), editable with a select editor, and updates via PUT to the API.
- Filters: Filter panel now includes Assigned Agent filter and uses project_name consistently.

Open issues to debug Monday:
1) Grid dropdown not visible yet next to Project:
   - Verify AG Grid select editor availability in current package version and module registration (AllCommunityModule registered).
   - Confirm column order and that project_name vs project columns aren’t duplicated/overlapping.
   - Ensure the API returns assigned_agent in all queries and the grid really receives it (network tab).
   - Check if cellEditor: 'agSelectCellEditor' requires a specific import/version with React 19 + AG Grid version used.
   - Verify CSS/theme isn’t masking the editor (z-index, overflow on grid container).

2) API query construction:
   - We switched to a mix of template and unsafe for dynamic WHERE; consider refactoring to fully parameterized or using a query builder.

3) Types alignment:
   - Component Installation interface uses project_name; confirm everywhere else (filters, renderers) uses the same key.

4) WhatsApp/Velo chat group sync not working:
   - Sync with Velo chat group is broken and installations from that chat are not showing up in the grid.
   - Need to debug the WhatsApp bridge/MCP integration and drop monitor service.
   - Check system status dashboard shows some services as stale/error.

Quick sanity checks for Monday:
- Hit GET /api/installations in browser and confirm assigned_agent present.
- In the grid, enable debug logging and ensure columnDefs contain assigned_agent and editable true.
- Temporarily render assigned_agent as plain text to confirm data binds, then re-enable select editor.

Commit context: partial feature in place; UI dropdown still not visible — continue Monday.
